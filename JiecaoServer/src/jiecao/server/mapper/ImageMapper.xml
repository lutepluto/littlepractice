<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="jiecao.server.mapper.ImageMapper">

	<cache />
	
	<select id="getLiveImage" parameterType="int" 
		resultType="jiecao.server.domain.Image">
		SELECT * FROM image WHERE image_pid = #{image_pid} and image_status = 1 and
			image_timestamp = (SELECT MAX(image_timestamp) from image)
	</select>
	
	<select id="getUpcomingImage" parameterType="int" 
		resultType="jiecao.server.domain.Image">
		SELECT * FROM image WHERE image_pid = #{image_pid} and image_status = 0 and
			image_timestamp = (SELECT MAX(image_timestamp) from image)
	</select>
	
	<select id="getSelectionImageList" resultType="jiecao.server.domain.Image">
		SELECT * FROM image WHERE image_status = 2 
	</select>
	
</mapper>